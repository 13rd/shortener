# Shortly 🔗

**Shortly** - это современный сервис сокращения URL-ссылок, построенный на FastAPI с асинхронной архитектурой и PostgreSQL базой данных.

## 🚀 Функционал

### Основные возможности
- **Сокращение ссылок**: Генерация уникальных коротких URL из длинных ссылок
- **Перенаправление**: Автоматический редирект с короткой ссылки на оригинальную
- **Веб-интерфейс**: Удобный фронтенд для взаимодействия с сервисом
- **REST API**: Полноценный API для интеграции с другими сервисами

### API Эндпоинты

#### POST `/short_url`
Создает короткую ссылку для указанного URL.

**Request:**
```json
{
  "long_url": "https://example.com/very/long/url"
}
```

**Response:**
```json
{
  "slug": "abc123"
}
```

#### GET `/{slug}`
Выполняет перенаправление на оригинальный URL.

**Response:** HTTP 302 Redirect на оригинальный URL

## 🛠️ Технологический стек

### Backend
- **FastAPI** - современный веб-фреймворк для Python
- **SQLAlchemy 2.0** - ORM с асинхронной поддержкой
- **PostgreSQL** - реляционная база данных
- **AsyncPG** - асинхронный драйвер PostgreSQL
- **Uvicorn** - ASGI сервер
- **Pydantic** - валидация данных

### Frontend
- **HTML5/CSS3** - современный адаптивный дизайн
- **JavaScript (Vanilla)** - клиентская логика
- **CSS Grid/Flexbox** - responsive layout

### Инфраструктура
- **Docker & Docker Compose** - контейнеризация
- **PostgreSQL 17** - база данных в контейнере
- **Pytest** - тестирование
- **UV** - менеджер зависимостей

## 📁 Структура проекта

```
shortly/
├── src/
│   ├── database/
│   │   ├── db.py          # Конфигурация базы данных
│   │   ├── models.py      # SQLAlchemy модели
│   │   └── crud.py        # CRUD операции
│   ├── main.py            # FastAPI приложение и роуты
│   ├── service.py         # Бизнес-логика
│   ├── shortener.py       # Генерация слагов
│   └── exceptions.py      # Кастомные исключения
├── tests/
│   ├── conftest.py        # Фикстуры pytest
│   ├── test_api.py        # Тесты API
│   └── test_service.py    # Тесты сервисов
├── index.html             # Веб-интерфейс
├── docker-compose.yaml    # Docker конфигурация
├── pyproject.toml         # Зависимости проекта
└── pytest.ini            # Конфигурация pytest
```

## 🚀 Быстрый старт

### Требования
- Python 3.14+
- Docker & Docker Compose
- UV (рекомендуется)

### Установка и запуск

1. **Клонирование репозитория**
```bash
git clone <repository-url>
cd shortly
```

2. **Запуск базы данных**
```bash
docker-compose up -d
```

3. **Установка зависимостей**
```bash
uv sync
```

4. **Активация виртуального окружения**
```bash
source .venv/bin/activate
```

5. **Запуск приложения**
```bash
python src/main.py
```

6. **Открытие веб-интерфейса**
Откройте `index.html` в браузере или перейдите на `http://localhost:8000`

## 🧪 Тестирование

### Запуск тестов
```bash
pytest
```

### Тестовое покрытие
- API эндпоинты
- Бизнес-логика сервисов
- Работа с базой данных (используется SQLite для тестов)

## 📊 База данных

### Модель данных
```sql
CREATE TABLE short_urls (
    slug VARCHAR PRIMARY KEY,
    long_url VARCHAR NOT NULL
);
```

### Конфигурация
- **Хост**: localhost:6432
- **База**: postgres
- **Пользователь**: postgres
- **Пароль**: postgres

## 🔧 Конфигурация

### Переменные окружения
На текущий момент конфигурация задана в коде:
- Database URL: `postgresql+asyncpg://postgres:postgres@localhost:6432/postgres`
- API Base URL: `http://localhost:8000`

## 📋 Нереализованные задачи

Из комментариев в `main.py`:

### Валидация и безопасность
- [ ] **Валидация URL**: Проверка корректности URL, включая специальные схемы (например, `tg://`)
- [ ] **Обработка коллизий слагов**: Улучшенная обработка конфликтов при генерации
- [ ] **Select for update**: Использование блокировок для предотвращения гонок состояний

### Функциональность
- [ ] **Кастомные слаги**: Создание человекочитаемых коротких ссылок
- [ ] **Аналитика переходов**: Сбор статистики по кликам
- [ ] **Время жизни ссылки**: TTL для ссылок с автоматическим удалением
- [ ] **Генерация на основе URL**: Детерминированная генерация слагов из URL

### Производительность
- [ ] **Redis интеграция**: Кэширование популярных ссылок
- [ ] **Предварительная генерация**: Генерация пула слагов заранее
- [ ] **Логирование**: Системное логирование операций

### Архитектура
- [ ] **Инверсия зависимостей**: Рефакторинг с использованием DI контейнера
- [ ] **Обертка над сессией БД**: Кастомный класс для управления сессиями
- [ ] **Тестовая база данных**: Отдельная конфигурация для тестов

### Обработка ошибок
- [ ] **Обработка несуществующих URL**: Улучшенная обработка 404 ошибок
- [ ] **Детальная валидация**: Расширенная проверка входных данных

## 🤝 Вклад в проект

1. Fork репозитория
2. Создайте feature branch (`git checkout -b feature/AmazingFeature`)
3. Commit изменения (`git commit -m 'Add some AmazingFeature'`)
4. Push в branch (`git push origin feature/AmazingFeature`)
5. Откройте Pull Request

## 📄 Лицензия

Проект распространяется под лицензией MIT.

## 📞 Контакты

При возникновении вопросов или предложений, пожалуйста, создайте issue в репозитории.
